@model IEnumerable<ProyectoBiblioteca.Models.Roles>

@{
    ViewBag.Title = "Index";
}

<h2>Index</h2>
<h3 class="text-danger">@ViewBag.ErrorEliminar</h3>

<p>
    @Html.ActionLink("Create New", "Create")
</p>
<table class="table">
    <tr>
        <th>
            @Html.DisplayNameFor(model => model.Rol)
        </th>
        <th></th>
    </tr>

@foreach (var item in Model) {
    <tr>
        <td>
            @Html.DisplayFor(modelItem => item.Rol)
        </td>
        <td>
            @Html.ActionLink("Edit", "Edit", new { id=item.Id_rol }) |
            @Html.ActionLink("Details", "Details", new { id=item.Id_rol }) |
            @Html.ActionLink("Delete", "Delete", new { id=item.Id_rol })
        </td>
    </tr>
}

</table>
